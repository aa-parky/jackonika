{
  "name": "jackonika",
  "version": "0.1.0",
  "description": "MIDI In stompbox adapter for the Tonika rack.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild src/jackonika.js --bundle --format=iife --global-name=Jackonika --outfile=dist/jackonika.iife.js --minify",
    "postbuild": "node scripts/copy-css.js",
    "build:backplanika": "echo 'Backplanika is plain JS (rack/backplanika.js + rack/backplanika.iife.js) — no build needed.'",
    "build:all": "npm run build && npm run build:backplanika",
    "dev": "http-server -p 9125 .",
    "demo": "http-server -p 9126 demo",
    "dev:random": "http-server -p 0 ."
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "http-server": "^14.1.1"
  }
}
